[package]
name = "chakracore-sys"
version = "0.1.0"
edition = "2024"
links = "chakracore-sys"

[dependencies]
chakra = { path = "../chakra" }
cxx = "1.0.194"

[build-dependencies]
cmake = "0.1.57"
cxx-build = { version = "1.0.194", features = ["parallel"] }

[features]
optimized-tests = []
